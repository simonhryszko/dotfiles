-- Configure LazyVim to use conform.nvim for formatting
-- local lsp_config = require("lazyvim.config.lsp")

-- Disable the LSP formatting capability to let conform.nvim handle it
-- lsp_config.capabilities.documentFormattingProvider = false
-- lsp_config.capabilities.documentRangeFormattingProvider = false
